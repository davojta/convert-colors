/**
 * @param value - current color value
 * @param targetSystem - if student can do programming and know basics
 * @returns string with value for color in target color system
 */
module.exports = function convertColor(
    value = "",
    targetSystem = "rgb"
) {
    return '';
};
  